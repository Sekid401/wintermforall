# wintermforall
A file that mimics the classic cmd.exe commands and the filesystem

# Who Owns It

Commands & Filesystem is owned by microsoft... not really but cmd.exe is owned by Microsoft along with the filesystem so

# why

do you wanna run windows commands on linux? no? yeah it exists to run fake windows command

Note: tons of commands actually map to linux/unix commands
